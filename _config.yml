# Site settings
title: Paul Jinsol Lee
description: Portfolio - Intelligence and Cyber Operations
url: "https://Pauleegone.github.io"  # Update with your GitHub username
baseurl: ""  # Leave empty if using username.github.io, or add "/repo-name" if using project site

# Build settings
markdown: kramdown
theme: null  # We're using custom CSS

# Exclude files from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
